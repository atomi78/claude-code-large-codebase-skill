---
description: Build or update CODEBASE_MAP.md
argument-hint: [path or focus]
allowed-tools: Read, Grep, Glob, LS, Bash, Edit, MultiEdit, Write
---

# Map Codebase

Use the `codebase-map-builder` skill.

Build or update a concise `CODEBASE_MAP.md` for the current repository or `$ARGUMENTS` focus. Inspect top-level directories, workspace/package files, CI/deploy files, ownership files, and existing docs.

Prefer a compact table with path, purpose, owner, and local guide.
